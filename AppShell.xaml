<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="mauiProject.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:mauiProject"
    xmlns:views="clr-namespace:mauiProject.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="mauiProject">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <ShellContent
        Title="BrandList"
        ContentTemplate="{DataTemplate views:BrandList}"
        Route="BrandList" />

    <ShellContent
        Title="BrandDetail"
        ContentTemplate="{DataTemplate views:BrandDetail}"
        Route="BrandDetail" />

</Shell>
